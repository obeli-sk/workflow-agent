{
  "name": "workflow-agent",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "../../just-bash/packages/just-bash/node_modules/.bin/esbuild workflow/agent-loop-src.js --bundle --format=esm --platform=browser --target=es2022 --define:__BROWSER__=true --alias:node:async_hooks=../../just-bash/packages/just-bash/src/shims/browser-unsupported.js --alias:node:module=../../just-bash/packages/just-bash/src/shims/browser-unsupported.js --alias:node:zlib=./workflow/node-zlib-shim.js --external:obelisk-agent:* --banner:js='const __workflowNativeDate = Date; globalThis.Date = new Proxy(__workflowNativeDate, { apply: () => new __workflowNativeDate(0).toString(), construct: (target, args) => new target(...(args.length ? args : [0])) }); Date.now = () => 0;' --minify --outfile=workflow/agent-loop.js"
  }
}
