{
  "name": "workflow-agent",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild workflow/agent-loop-src.js --bundle --format=esm --platform=browser --target=es2022 --define:__BROWSER__=true --alias:node:async_hooks=./vendor/just-bash/src/shims/browser-unsupported.js --alias:node:module=./vendor/just-bash/src/shims/browser-unsupported.js --alias:node:zlib=./workflow/node-zlib-shim.js --external:obelisk-agent:* --banner:js='const __workflowNativeDate = Date; globalThis.Date = new Proxy(__workflowNativeDate, { apply: () => new __workflowNativeDate(0).toString(), construct: (target, args) => new target(...(args.length ? args : [0])) }); Date.now = () => 0;' --minify --outfile=workflow/agent-loop.js"
  },
  "devDependencies": {
    "esbuild": "^0.27.7"
  }
}
